Your task is to implement AgBlogger following PLAN.md. Update PLAN.md periodically to reflect progress and changes. After completing each phase, write tests and ensure they pass. Review the code after each phase to ensure it meets the requirements and is well-structured.

## Python requirements:
- Use strict typing with mypy
- Use type hints for function parameters and return types
- Use docstrings for functions and classes
- Use type aliases for complex types
- Use type annotations for variables and attributes
